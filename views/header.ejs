<!DOCTYPE html>
<html lang="en">

<head>
    <!-- Tags to add responsiveness to the website -->
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Author -->
    <meta name="--Author--" content="Bravine Cheruiyot">
    <meta name="description" content="Sign up for ShareLyft.com">

    <!-- Title -->
    <title>
        <%= title %>
    </title>

    <!-- Dynamic Custom CSS -->
    <link rel="stylesheet" href="/<%= cssFile %>"> <!-- Correct path for the CSS -->
    <!-- ✅ Fix: Only include JS file if it is defined -->
    <% if (jsFile) { %>
        <script src="/<%= jsFile %>" defer></script>
        <% } %> <!-- Correct path for the JS -->

            <!-- To support bootstrap in my website -->
            <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<!DOCTYPE html>